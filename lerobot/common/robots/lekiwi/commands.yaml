python -m lerobot.record    --robot.type=lekiwi   --robot.port=/dev/ttyACM0   --robot.cameras="{ front: {type: opencv, index_or_path: /dev/video0, width: 640, height: 480, fps: 30}, wrist: {type: opencv, index_or_path: /dev/video1, width: 640, height: 480, fps: 30, rotation: ROTATE_180}}"   --robot.id=rosey   --teleop.type=so100_leader   --teleop.port=/dev/ttyACM0   --teleop.id=rosey_master   --display_data=false   --dataset.repo_id=nikx-vla/eval_so100   --dataset.single_task="Put lego brick into the transparent box"   --policy.path=orsoromeo/smolvla_so101_pick_and_place

python -m lerobot.teleoperate     --robot.type=lekiwi     --robot.cameras="{        front: {type: opencv, index_or_path: /dev/video0, width: 640, height: 480, fps: 30},        wrist: {type: opencv, index_or_path: /dev/video1, width: 640, height: 480, fps: 30, rotation: ROTATE_180}    }"     --robot.id=rosey     --teleop.type=so100_leader     --teleop.port=/dev/ttyACM0     --teleop.id=rosey_master     --display_data=true

python -m lerobot.record    --robot.type=lekiwi   --robot.cameras="{ front: {type: opencv, index_or_path: /dev/video0, width: 640, height: 480, fps: 30}, wrist: {type: opencv, index_or_path: /dev/video1, width: 640, height: 480, fps: 30, rotation: ROTATE_180}}"   --robot.id=rosey   --display_data=false   --dataset.repo_id=nikx-vla/eval_so100   --dataset.single_task="Put lego brick into the transparent box"   --policy.path=lerobot/smolvla_base